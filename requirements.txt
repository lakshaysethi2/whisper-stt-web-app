fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
aiofiles==24.1.0
pydantic==2.9.0
httpx==0.28.1
faster-whisper==1.2.1
# CrisperWhisper 2.0 verbatim ASR (models: nyralabs/CrisperWhisper2.0_{large,turbo,medium,small}).
# Uses the pure-PyTorch backend on purpose: the [ct2] extra conflicts with
# faster-whisper (its upstream ctranslate2 overwrites the ctranslate2-crisperwhisper
# fork's files) and its wheels are Linux x86_64 only (not installable on the
# reference ARM64 host). See README "CrisperWhisper 2.0" section for GPU/CPU tradeoffs.
crisperwhisper[transformers]==2.0.1
# Parakeet TDT 0.6B v3 (nvidia/parakeet-tdt-0.6b-v3) via transformers pipeline.
# Shares torch/transformers/accelerate with crisperwhisper[transformers];
# pinned to avoid pulling incompatible minor versions on ARM.
# NOTE: keep `transformers>=4.57` or newer — Parakeet needs the ParakeetForTDT backend
# added in 4.57. soundfile is already pulled by crisperwhisper.
# Parakeet runtime is optional: app boots with a clear "parakeet not available" when missing.
transformers>=4.57
# Parakeet feature extractor needs librosa (not pulled by crisperwhisper[transformers]).
librosa>=0.11
soundfile>=0.13
# Node config loader (app/nodes.py reads config/nodes.yaml).
pyyaml>=6.0
